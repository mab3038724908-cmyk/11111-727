# 给离线算法开发者的任务书

## 1. 任务目的

在不接触机器人、不登录 Jetson、不运行 ROS 的前提下，改进大型室内清扫机器人的覆盖路径规划，并为前部可移动清扫装置建立可执行的离线规划接口。

机器人约长 `1.2m`、宽 `0.8m`。当前算法已经能把不规则区域按门口、房间和走廊分区，并保证一个区域完整清扫后才去下一个区域。现在需要继续减少交叉、重复通行和无效碎片，同时利用可移动清扫头提高墙边覆盖、减少底盘贴墙和不必要转向。

## 2. 必须保留的现有能力

1. 从机器人当前位置选择真实可达区域，禁止跳到不连通的大区域。
2. 未知格和障碍格不可通行。
3. 使用完整非对称车体，而不是圆形机器人近似。
4. 一个区域必须按 `fill → perimeter → transfer` 完成，不能清到一半跳去别区再回来。
5. 区域内扫带平行、间距均匀，方向优先沿区域长边。
6. 门口和走廊连接必须完整车体无碰撞、路径连续。
7. 学习或搜索产生的候选必须经过确定性安全校验，不能绕过硬约束。

## 3. 主要优化目标

按优先级排序：

1. 完整车体碰撞数始终为 0。
2. 可服务地面覆盖率不低于 95%。
3. 所有可访问区域完整完成，且区域访问顺序不反复。
4. 降低非必要几何交叉和已完成路线重复。
5. 减少区域间连接长度、掉头和中央走廊重复通行。
6. 在相同硬约束下缩短规划时间。
7. 将可移动清扫头纳入状态、碰撞、覆盖和执行时序。

map9 当前实测参考值：路线约 `466.32m`，非必要交叉参考值 `14`，可服务地面覆盖约 `98.43%`，实际刷洗覆盖约 `96.99%`，完整车体碰撞 `0`。优化结果不得用降低安全性或覆盖率换取更好数字。规划耗时必须在同一台离线电脑上前后对比，不能拿电脑耗时直接推算车端耗时。

## 4. 可移动前清扫装置改造

现有代码已经提供模拟语义：

- `EDGE_LEFT`
- `EDGE_RIGHT`
- `EDGE_CENTER`
- `EDGE_RETRACT`
- `EDGE_IGNORE`
- 每个路径点有 `offset_m`，正值为车体左侧

但当前只是离线元数据，默认最大横移约 `±0.25m`，没有真实执行器速度、加速度、时延、反馈、限位或故障状态。

开发者需要把输出提升为联合轨迹：

```text
(x, y, yaw, cleaner_offset, cleaner_mode, cleaning_enabled)
```

硬件参数尚未最终提供时，不得猜测。必须把行程、速度、加速度、装置尺寸、碰撞外形、伸出方向、限位、反馈频率和失效策略做成显式配置，并提供参数缺失时的 fail-closed 行为。

完整要求见 `docs/MOVABLE_FRONT_CLEANER.md`。

## 5. 交付物

1. 修改后的源码或标准 Git patch。
2. 新增/更新的测试。
3. map9 和合成场景的前后指标 JSON。
4. 路径预览图，能区分填充、沿边、区域连接和清扫头中心轨迹。
5. 算法说明：改变了什么、为什么、复杂度和退化/失败策略。
6. 清扫头接口说明和完整参数表。
7. 不依赖机器人即可复现的运行命令。

## 6. 禁止事项

- 不申请或使用机器人 SSH。
- 不写入车端目录。
- 不发布 ROS 速度、导航目标或清扫控制。
- 不删除未知区/障碍检查以提高覆盖率。
- 不把尚未校验的学习策略作为安全控制器。
- 不以“图片看起来更整齐”代替量化测试。
